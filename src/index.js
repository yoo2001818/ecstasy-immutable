import createStore from './createStore.js';
import { createAction } from 'redux-actions';
import * as builtIn from './builtIn.js';

export default createStore;
export { createStore, createAction, builtIn };
